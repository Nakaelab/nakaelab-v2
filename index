<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Digital Twin Laboratory - Nakae Lab</title>
  
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Helvetica Neue', Arial, sans-serif;
      background: #000;
      color: #fff;
      overflow-x: hidden;
    }

    /* カーソルエフェクト用 */
    .cursor-glow {
      position: fixed;
      width: 400px;
      height: 400px;
      border-radius: 50%;
      background: radial-gradient(
        circle,
        rgba(102, 126, 234, 0.4) 0%,
        rgba(118, 75, 162, 0.2) 50%,
        transparent 100%
      );
      pointer-events: none;
      transform: translate(-50%, -50%);
      filter: blur(80px);
      mix-blend-mode: screen;
      z-index: 1;
      transition: opacity 0.3s ease;
    }

    /* ヒーローセクション */
    .hero {
      position: relative;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 60px 20px;
      background: linear-gradient(
        180deg,
        #000 0%,
        #0a0a1a 50%,
        #1a0a2e 100%
      );
      overflow: hidden;
    }

    .hero-content {
      position: relative;
      z-index: 2;
      max-width: 1000px;
    }

    .hero-label {
      font-size: 14px;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: rgba(255, 255, 255, 0.5);
      margin-bottom: 30px;
      animation: fadeInUp 1s ease 0.2s forwards;
      opacity: 0;
    }

    .hero-title {
      font-size: clamp(48px, 8vw, 96px);
      font-weight: 300;
      line-height: 1.1;
      margin-bottom: 40px;
      letter-spacing: -0.02em;
      animation: fadeInUp 1s ease 0.4s forwards;
      opacity: 0;
    }

    .hero-title strong {
      font-weight: 600;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .hero-subtitle {
      font-size: clamp(20px, 3vw, 32px);
      font-weight: 300;
      line-height: 1.6;
      color: rgba(255, 255, 255, 0.7);
      margin-bottom: 60px;
      animation: fadeInUp 1s ease 0.6s forwards;
      opacity: 0;
    }

    .hero-cta {
      display: inline-block;
      padding: 18px 40px;
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.2);
      border-radius: 50px;
      color: #fff;
      text-decoration: none;
      font-size: 16px;
      font-weight: 500;
      transition: all 0.3s ease;
      animation: fadeInUp 1s ease 0.8s forwards;
      opacity: 0;
    }

    .hero-cta:hover {
      background: rgba(255, 255, 255, 0.2);
      border-color: rgba(255, 255, 255, 0.4);
      transform: translateY(-2px);
      box-shadow: 0 10px 40px rgba(102, 126, 234, 0.3);
    }

    .scroll-indicator {
      position: absolute;
      bottom: 40px;
      left: 50%;
      transform: translateX(-50%);
      color: rgba(255, 255, 255, 0.3);
      font-size: 12px;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      animation: bounce 2s infinite;
    }

    /* セクション */
    section {
      padding: 120px 20px;
      position: relative;
      z-index: 2;
    }

    .section-title {
      font-size: clamp(36px, 5vw, 64px);
      font-weight: 300;
      text-align: center;
      margin-bottom: 80px;
      letter-spacing: -0.02em;
    }

    /* 研究の柱 */
    .pillars {
      max-width: 1200px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 40px;
    }

    .pillar {
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 20px;
      padding: 40px;
      transition: all 0.3s ease;
    }

    .pillar:hover {
      background: rgba(255, 255, 255, 0.08);
      border-color: rgba(102, 126, 234, 0.3);
      transform: translateY(-5px);
      box-shadow: 0 20px 60px rgba(102, 126, 234, 0.2);
    }

    .pillar-number {
      font-size: 14px;
      color: rgba(255, 255, 255, 0.4);
      margin-bottom: 20px;
      letter-spacing: 0.1em;
    }

    .pillar h3 {
      font-size: 24px;
      font-weight: 600;
      margin-bottom: 20px;
      color: #fff;
    }

    .pillar p {
      font-size: 16px;
      line-height: 1.8;
      color: rgba(255, 255, 255, 0.7);
    }

    /* アニメーション */
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes bounce {
      0%, 100% {
        transform: translateX(-50%) translateY(0);
      }
      50% {
        transform: translateX(-50%) translateY(-10px);
      }
    }

    /* レスポンシブ */
    @media (max-width: 768px) {
      .hero {
        padding: 40px 20px;
      }
      
      .hero-title {
        font-size: 40px;
      }
      
      .hero-subtitle {
        font-size: 18px;
      }
      
      section {
        padding: 80px 20px;
      }
      
      .pillars {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <!-- カーソルエフェクト -->
  <div class="cursor-glow"></div>

  <!-- ヒーローセクション -->
  <section class="hero">
    <div class="hero-content">
      <div class="hero-label">福井大学 中江研究室</div>
      <h1 class="hero-title">
        <strong>Digital Twin</strong><br>
        Laboratory
      </h1>
      <p class="hero-subtitle">
        生命をデジタル空間で再現する<br>
        AIで解き明かす、脳と行動のダイナミクス
      </p>
      <a href="#research" class="hero-cta">研究内容を見る</a>
    </div>
    <div class="scroll-indicator">Scroll to explore ↓</div>
  </section>

  <!-- 研究セクション -->
  <section id="research">
    <h2 class="section-title">研究の3本柱</h2>
    <div class="pillars">
      <div class="pillar">
        <div class="pillar-number">01</div>
        <h3>生命のデジタルツイン</h3>
        <p>動物やヒトの感覚と行動の長期計測から、そのダイナミクスを人工知能技術などを用いてモデル化しデジタル空間で自発的に生きる生命を作ります。</p>
      </div>
      <div class="pillar">
        <div class="pillar-number">02</div>
        <h3>脳神経データ解析</h3>
        <p>デジタルツインやデータベースを用いてアルツハイマー病やてんかんといった神経内科学(Neurology)分野を理論的に理解するための枠組みを発展させます。</p>
      </div>
      <div class="pillar">
        <div class="pillar-number">03</div>
        <h3>Computational Neurology</h3>
        <p>大規模な脳・行動データを解析し、生命の複雑さのアーキテクチャを解読します。</p>
      </div>
    </div>
  </section>

  <script>
    // カーソルエフェクト
    const cursorGlow = document.querySelector('.cursor-glow');
    
    document.addEventListener('mousemove', (e) => {
      cursorGlow.style.left = e.clientX + 'px';
      cursorGlow.style.top = e.clientY + 'px';
      cursorGlow.style.opacity = '1';
    });

    document.addEventListener('mouseleave', () => {
      cursorGlow.style.opacity = '0';
    });

    // スムーススクロール
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
          target.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
        }
      });
    });
  </script>
</body>
</html>
